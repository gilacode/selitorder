
# ----------------------
# Notification
# ----------------------

# Get notifications by Category
# onlyNew = Filter only unread notifications
GET     /users/:username/notifs                         com.avicenna.notification.api.NotifApiCtrl.get(username)

# Get notifications by Category
# onlyNew = Filter only unread notifications
GET     /users/:username/notifs/category/:category      com.avicenna.notification.api.NotifApiCtrl.getByCategory(username, category)